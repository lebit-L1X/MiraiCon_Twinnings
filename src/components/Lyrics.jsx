export const Lyrics = ({ text }) => {
  return (
    <div
      style={{
        fontSize: "5rem",
        textAlign: "center",
        marginBottom: "2rem",
      }}
    >
      {text}
    </div>
  );
};